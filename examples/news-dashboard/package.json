{
  "name": "news-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Nexus demo — SSR news from Hacker News API (no client island)",
  "scripts": {
    "dev": "nexus dev --port 3030",
    "build": "nexus build",
    "start": "nexus start --port 3030"
  },
  "dependencies": {
    "@nexus_js/head": "workspace:*",
    "@nexus_js/runtime": "workspace:*",
    "@nexus_js/server": "workspace:*"
  },
  "devDependencies": {
    "@nexus_js/cli": "workspace:*",
    "@nexus_js/compiler": "workspace:*",
    "typescript": "^5.5.0"
  }
}
