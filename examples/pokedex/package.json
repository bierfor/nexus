{
  "name": "pokedex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --watch-path=./src server.mjs",
    "start": "node server.mjs",
    "build": "nexus build"
  },
  "dependencies": {
    "@nexus_js/runtime": "workspace:*",
    "@nexus_js/serialize": "workspace:*"
  },
  "devDependencies": {
    "@nexus_js/cli": "workspace:*",
    "@nexus_js/compiler": "workspace:*",
    "typescript": "^5.5.0"
  }
}
