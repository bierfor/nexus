{
  "name": "anonymous-chat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Anonymous realtime chat — Nexus SSR + Connect (SSE) + server actions",
  "scripts": {
    "dev": "nexus dev --port 3020",
    "build": "nexus build",
    "start": "nexus start --port 3020"
  },
  "dependencies": {
    "@nexus/connect": "workspace:*",
    "@nexus/head": "workspace:*",
    "@nexus/runtime": "workspace:*",
    "@nexus/serialize": "workspace:*",
    "@nexus/server": "workspace:*"
  },
  "devDependencies": {
    "@nexus/cli": "workspace:*",
    "@nexus/compiler": "workspace:*",
    "typescript": "^5.5.0"
  }
}
