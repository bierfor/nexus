{
  "name": "nexusflow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "NexusFlow — demo app driven by @nexus_js/compiler + nexus dev (SSR from .nx)",
  "scripts": {
    "dev": "nexus dev --port 3010",
    "build": "nexus build",
    "start": "nexus start --port 3010",
    "audit": "nexus audit",
    "check": "nexus check"
  },
  "dependencies": {
    "@nexus_js/head": "workspace:*",
    "@nexus_js/runtime": "workspace:*",
    "@nexus_js/serialize": "workspace:*"
  },
  "devDependencies": {
    "@nexus_js/cli": "workspace:*",
    "@nexus_js/compiler": "workspace:*",
    "typescript": "^5.5.0"
  }
}
